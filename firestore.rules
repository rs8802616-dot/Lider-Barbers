rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    function isAuthenticated() {
      return request.auth != null;
    }
    
    function isOwner(userId) {
      return isAuthenticated() && request.auth.uid == userId;
    }

    // Catálogo de serviços é público para leitura e gestão
    match /servicos/{servicoId} {
      allow read: if true;
      allow write: if true;
    }

    // Barbeiros disponíveis para consulta pública e gestão
    match /barbeiros/{barbeiroId} {
      allow read: if true;
      allow write: if true;
    }

    // Disponibilidades e bloqueios são lidos para cálculo de horários
    match /disponibilidades/{disponibilidadeId} {
      allow read: if true;
      allow write: if true;
    }

    match /bloqueios/{bloqueioId} {
      allow read: if true;
      allow write: if true;
    }

    // Usuários podem ler e escrever seu próprio perfil
    match /users/{userId} {
      allow read: if true;
      allow write: if true;
    }

    // Agendamentos: clientes podem criar suas reservas e ler seus agendamentos; barbeiros e admins gerenciam
    match /agendamentos/{agendamentoId} {
      allow read: if true;
      allow write: if true;
    }

    // Notificações
    match /notificacoes/{notificacaoId} {
      allow read: if true;
      allow write: if true;
    }
  }
}

